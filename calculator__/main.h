#ifndef MAIN_H
#define MAIN_H
#include <stdio.h>
#include <math.h>
void  sum (double a,double b,char op);
void  sub (double a,double b,char op);
void  mult (double a,double b,char op);
void  power(double a,double b,char op);
void  dev (double a,double b,char op);
void  root (double a,double b,char op);
void calculator (double a,double b,char op);

#endif // !MAIN_H
